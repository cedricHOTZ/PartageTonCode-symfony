{% extends 'base.html.twig' %}

{% block title 'Editer un pin' ~ pin.id %}


{% block body %}
<article>

<h1>Editer pin{{pin.id}}</h1>
{{form_start(form)}}

{{form_widget(form)}}
<input type="submit" value="Modifier le pin"/>
{{ form_end(form) }}
</article>

 {% endblock %}